/** Campos de canal e-mail do agente (Gmail OAuth). */
export type AgenteEmailSnapshot = {
  email_from?: string | null;
  email_from_name?: string | null;
  email_inbound?: string | null;
  email_ativo?: boolean;
};
